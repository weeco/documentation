---
title: rpk cluster storage recovery start
rpk_version: (rev )
---

Start the topic recovery process.
This command starts the process of restoring topics from the archival bucket.
If the wait flag (--wait/-w) is set, the command will poll the status of the
recovery process until it's finished.

## Usage

```bash
rpk cluster storage recovery start [flags]
```

## Flags

<table>
  <tbody>
    <tr>
      <td>
        <strong>Value</strong>
      </td>
      <td>
        <strong>Type</strong>
      </td>
      <td>
        <strong>Description</strong>
      </td>
    </tr>
    <tr>
      <td>-h, --help</td>
      <td>-</td>
      <td>Help for start.</td>
    </tr>
    <tr>
      <td>--polling-interval</td>
      <td>duration</td>
      <td>
        The status check interval (e.g. '30s', '1.5m'); ignored if --wait is not
        used (default 5s).
      </td>
    </tr>
    <tr>
      <td>--topic-name-pattern</td>
      <td>string</td>
      <td>A regex pattern that restores any matching topics (default ".*").</td>
    </tr>
    <tr>
      <td>-w, --wait</td>
      <td>-</td>
      <td>Wait until auto-restore is complete.</td>
    </tr>
    <tr>
      <td>--admin-api-tls-cert</td>
      <td>string</td>
      <td>
        The certificate to be used for TLS authentication with the admin API.
      </td>
    </tr>
    <tr>
      <td>--admin-api-tls-enabled</td>
      <td>-</td>
      <td>
        Enable TLS for the Admin API (not necessary if specifying custom certs).
      </td>
    </tr>
    <tr>
      <td>--admin-api-tls-key</td>
      <td>string</td>
      <td>
        The certificate key to be used for TLS authentication with the admin
        API.
      </td>
    </tr>
    <tr>
      <td>--admin-api-tls-truststore</td>
      <td>string</td>
      <td>
        The CA certificate to be used for TLS communication with the admin API.
      </td>
    </tr>
    <tr>
      <td>--admin-url</td>
      <td>strings</td>
      <td>.</td>
    </tr>
    <tr>
      <td>--api-urls</td>
      <td>strings</td>
      <td>Comma separated list of admin API host:ports.</td>
    </tr>
    <tr>
      <td>--config</td>
      <td>string</td>
      <td>
        Redpanda or rpk config file; default search paths are
        ~/.config/rpk/rpk.yaml, $PWD, and /etc/redpanda/`redpanda.yaml`.
      </td>
    </tr>
    <tr>
      <td>-X, --config-opt</td>
      <td>stringArray</td>
      <td>
        Override rpk configuration settings; '-X help' for detail or '-X list'
        for terser detail.
      </td>
    </tr>
    <tr>
      <td>--hosts</td>
      <td>strings</td>
      <td>.</td>
    </tr>
    <tr>
      <td>-v, --verbose</td>
      <td>string</td>
      <td>
        [="info"] Log level (none, error, warn, info, debug) (default "none").
      </td>
    </tr>
  </tbody>
</table>
