---
title: rpk profile edit
rpk_version: (rev )
---

Edit an rpk profile.

This command opens your default editor to edit the specified profile, or
the current profile if no profile is specified.

If you are editing the current profile, rpk also populates what you edit
with internal defaults, user specified flags, and environment variables.
If you want to edit the current raw profile as it exists in rpk.yaml, you
can use the --raw flag.

## Usage

```bash
rpk profile edit [NAME] [flags]
```

## Flags

<table>
  <tbody>
    <tr>
      <td>
        <strong>Value</strong>
      </td>
      <td>
        <strong>Type</strong>
      </td>
      <td>
        <strong>Description</strong>
      </td>
    </tr>
    <tr>
      <td>-h, --help</td>
      <td>-</td>
      <td>Help for edit.</td>
    </tr>
    <tr>
      <td>--raw</td>
      <td>-</td>
      <td>
        Edit profile directly as it exists in rpk.yaml without any environment
        variables nor flags applied.
      </td>
    </tr>
    <tr>
      <td>--config</td>
      <td>string</td>
      <td>
        Redpanda or rpk config file; default search paths are
        ~/.config/rpk/rpk.yaml, $PWD, and /etc/redpanda/`redpanda.yaml`.
      </td>
    </tr>
    <tr>
      <td>-X, --config-opt</td>
      <td>stringArray</td>
      <td>
        Override rpk configuration settings; '-X help' for detail or '-X list'
        for terser detail.
      </td>
    </tr>
    <tr>
      <td>-v, --verbose</td>
      <td>string</td>
      <td>
        [="info"] Log level (none, error, warn, info, debug) (default "none").
      </td>
    </tr>
  </tbody>
</table>
